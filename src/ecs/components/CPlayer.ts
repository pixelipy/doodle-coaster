import { Component } from "../core/component";

export class CPlayer extends Component {
    // This component can be used to tag an entity as a player, and store player-specific data if needed
    // For now, it's just an empty component used for tagging
    constructor() {
        super();
    }
}