import { Component } from "../core/component";

//tag component for hiding entities. Systems can check for this component and hide the entity's mesh or skip rendering it.
export class CHide extends Component{}